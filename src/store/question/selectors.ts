export const selectIsLoadingQuestion = (state) => state.question.isLoading;
export const selectCurrentQuestion = (state) => state.question.currentQuestion;
export const selectIsInitializing = (state) => state.question.isInitializing;
export const selectIsUpdatingSkipped = (state) => state.question.isUpdatingSkipped;
